# MOBILE FIRST IA AND PC RESTRUCTURE ROADMAP (2026-05-04)

- 목적: 첨부 목업의 직관적 모바일 UX와 현재 NEW-PSI의 실제 기능 자산을 결합해, 모바일 우선 재구성과 PC 후속 재편의 실행 기준을 정의한다.
- 전략 요약: **모바일은 행동 중심**, **PC는 운영 중심**으로 분리 설계한다.
- 기본 판단: 현재 프로그램은 기능 자산이 강하고, 첨부 목업은 진입 UX가 강하다. 따라서 `기능은 유지`, `사용경험은 재설계`가 최적이다.

---

## 1) 핵심 결론

### 1-1. 왜 모바일 먼저인가
1. 현장 사용자는 기능 탐색보다 즉시 행동이 중요하다.
2. 현재 구조는 정보량이 많아 모바일에서 인지 부하가 높다.
3. 첨부 목업은 `첫 진입 → OCR 시작 → AI 리스크 확인` 흐름이 매우 짧고 명확하다.
4. 모바일 UX 기준이 먼저 고정돼야 이후 PC 재구성도 우선순위가 정리된다.

### 1-2. 왜 PC는 후속인가
- PC는 단순 축소판이 아니라 별도 목적 화면이어야 한다.
- 관리자/운영자/보고/배치 작업은 넓은 화면에서 생산성 중심으로 재구성하는 편이 낫다.
- 모바일 정보구조를 먼저 고정한 뒤, PC는 그 위에 관리 기능을 확장하는 방식이 안전하다.

---

## 2) 현재 프로그램과 목업의 콜라보 원칙

### 2-1. 유지할 것 (엔진)
- OCR 분석 엔진
- AI 리스크 산정 로직
- 보고서/기록 데이터 구조
- 관리자 승인/검토 흐름
- 다국어/10개국 QA 기준

### 2-2. 재설계할 것 (경험)
- 첫 화면 정보구조
- 모바일 네비게이션 방식
- CTA 우선순위
- 카드 밀도와 텍스트 길이
- 단계형 액션 흐름
- 테마별 시각 표현 방식

### 2-3. 해석 원칙
- 현재 프로그램은 `기능 플랫폼`
- 첨부 목업은 `모바일 사용경험 표준안`
- 따라서 목표는 "현재 기능을 버리는 것"이 아니라 "모바일에서 누구나 바로 쓰게 만드는 것"이다.

---

## 3) 모바일 우선 정보구조(IA)

## 3-1. 모바일 메인 탭 구조
1. `홈`
2. `분석`
3. `리포트`
4. `근로자`
5. `더보기`

### 3-2. 홈 화면 역할
- 오늘 상태 3초 이해
- 전체 현황 / 위험군 / 안전 상태 확인
- 최근 분석 리포트 확인
- `시작하기` CTA로 OCR 진입

### 3-3. OCR 화면 역할
- 수기 기록지 분석 시작
- 분석 전 체크리스트 확인
- 파일 업로드/카메라/권한 흐름 최소화

### 3-4. AI 리스크 화면 역할
- 리스크 점수 즉시 이해
- 적/황/녹 요약 확인
- 경향/개별 점검/AI 인사이트로 분기

### 3-5. 더보기 역할
- 설정
- 관리자 기능
- 고급 리포트
- 로그/운영 메뉴
- PC급 상세 기능으로 연결되는 보조 진입점

---

## 4) 모바일 UX 설계 원칙

### 4-1. 구조 원칙
- 첫 화면에서 CTA는 1개 우선 노출
- 핵심 수치는 3개 이하로 제한
- 상세 기능은 접기/펼치기 또는 다음 화면으로 분리
- 하단 탭바 고정
- 각 화면별 고정 CTA 1개 유지

### 4-2. 사용성 원칙
- 한 손 조작 기준
- 터치 타겟 44x44 이상
- 숫자 우선, 설명은 짧게
- 색상만으로 의미 전달 금지
- 단계별 진행 흐름 유지

### 4-3. 직관성 원칙
- `어디서 뭘 해야 하는지`를 설명하지 않아도 보이게 설계
- 기능명을 나열하지 말고 행동을 보여주기
  - 예: `OCR 분석`보다 `분석 시작`
  - 예: `AI 리스크`보다 `위험 확인`

### 4-4. 테마 원칙
- 다크 / 라이트 / 혼합 모드는 같은 구조를 공유
- 테마는 감성 차이, 구조는 고정
- 경고/위험 카드만 강조 강도 차등 적용

---

## 5) PC 후속 재구성 원칙

### 5-1. PC의 역할 재정의
PC는 모바일 확대판이 아니라 운영 콘솔이어야 한다.

### 5-2. PC 우선 기능군
- 대량 OCR 처리
- 필터/검색/정렬
- 관리자 승인/반려/재분석
- 보고서 생성/배포
- 근로자 이력/비교 분석
- 정책/품질/운영 로그

### 5-3. PC 재편 방향
- 대시보드: 요약 + 드릴다운 중심
- OCR: 배치 처리 중심
- AI 리스크: 비교/우선순위/후속조치 중심
- 리포트: 출력/배포/이력 중심
- 설정: 운영 규칙/권한/연동 중심

### 5-4. PC 레이아웃 원칙
- 좌측 기능 네비 + 우측 작업 영역
- 표/패널/필터 고정
- 모바일에서 숨긴 상세 데이터 적극 노출
- 멀티패널 구조 허용

---

## 6) 단계별 실행 로드맵

### Phase 1. 모바일 우선 3화면 완성
대상:
- Dashboard(홈)
- OcrAnalysis
- PredictiveAnalysis

목표:
- 첨부 목업 수준의 진입 UX 확보
- 하단 탭/고정 CTA/핵심 요약 구조 정착
- 다크/라이트/혼합 모드 동일 구조 유지

완료 기준:
- 320/360/375/390 뷰포트 사용성 통과
- 첫 행동 도달 시간 단축
- 핵심 화면 3개 직관성 확보

### Phase 2. 모바일 연결 흐름 확장
대상:
- 최근 리포트
- 분석 결과 상세
- 알림/상태
- 진행중 작업 이어보기

목표:
- 홈 → OCR → AI 리스크 → 리포트 흐름 자연 연결
- 사용자가 길을 잃지 않게 단계 전환 정리

### Phase 3. PC 기능 재구성
대상:
- Dashboard
- OCR 운영
- AI 리스크 운영
- Reports
- Settings/Admin

목표:
- 모바일과 다른 목적의 운영 콘솔 확립
- 대량 처리와 관리자 생산성 극대화

### Phase 4. 공통 디자인 시스템 정식화
대상:
- 간격/타이포/색상/상태칩/버튼/카드/게이지
- 탭바/헤더/패널/리스트

목표:
- 모바일/PC 공통 토큰화
- 향후 기능 추가 시 일관성 유지

---

## 7) 구현 우선순위

### 최우선
1. 모바일 홈 첫 화면 직관성
2. OCR 시작 흐름 단순화
3. AI 리스크 점수/버킷/액션 구조 고정

### 중간 우선순위
4. 최근 리포트 연결
5. 모바일 더보기 메뉴 정리
6. 상태/알림/진행중 작업 표시

### 후순위
7. PC 관리자 콘솔 재편
8. 고급 보고/배치 처리 분리
9. 운영 규칙/품질 로그 UI 재정렬

---

## 8) 성공 기준

### 모바일 성공 기준
- 처음 보는 사용자도 10초 내 시작 동작 가능
- 3개 핵심 화면에서 행동 버튼 위치가 일관됨
- 위험/주의/안전 상태를 텍스트 없이도 대략 이해 가능
- 실측 QA에서 오터치/겹침/가독성 이슈 최소화

### PC 성공 기준
- 운영자가 한 화면에서 더 많은 판단과 처리를 할 수 있음
- 배치 처리/필터/보고서 작업 시간이 감소
- 모바일과 역할이 겹치지 않음

---

## 9) 주의사항
- PC 구조를 모바일에 축소 이식하면 실패 가능성 높음
- 모바일에 현재 모든 기능을 그대로 노출하면 직관성 붕괴
- 테마보다 먼저 정보구조를 고정해야 함
- 기능 이름보다 행동 흐름을 먼저 설계해야 함

---

## 10) 최종 제안
- 지금 방향은 맞다.
- 모바일은 첨부 목업을 기준으로 `행동 중심 재설계`
- PC는 이후 `기능 중심 재편`
- 현재 프로그램은 엔진으로 유지하고, 사용자 경험만 단계적으로 재구성하는 것이 가장 현실적이고 성공 확률이 높다.

---

## 11) 다음 실행 항목
1. 모바일 홈/분석/OCR 3화면을 목업 기준 공통 IA로 다시 정렬
2. 하단 탭 및 CTA 우선순위 정식 확정
3. 모바일 연결 흐름(리포트/상세/알림) 설계
4. PC 후속 재편용 기능군 분류표 작성

---

## 12) 연결 문서
- [MOBILE_HOME_ANALYSIS_OCR_DETAILED_IA_TABLE_2026-05-04.md](MOBILE_HOME_ANALYSIS_OCR_DETAILED_IA_TABLE_2026-05-04.md)
- [PC_FUNCTION_RECLASSIFICATION_TABLE_2026-05-04.md](PC_FUNCTION_RECLASSIFICATION_TABLE_2026-05-04.md)
- [MOBILE_SCREEN_WIREFRAME_COPY_DRAFT_2026-05-04.md](MOBILE_SCREEN_WIREFRAME_COPY_DRAFT_2026-05-04.md)
- [CURRENT_PAGE_TO_PC_MENU_MAPPING_TABLE_2026-05-04.md](CURRENT_PAGE_TO_PC_MENU_MAPPING_TABLE_2026-05-04.md)
- [APP_MENU_RESTRUCTURE_PROPOSAL_2026-05-04.md](APP_MENU_RESTRUCTURE_PROPOSAL_2026-05-04.md)
- [MOBILE_3CORE_SCREEN_IMPLEMENTATION_CHECKLIST_2026-05-04.md](MOBILE_3CORE_SCREEN_IMPLEMENTATION_CHECKLIST_2026-05-04.md)

---

## 13) 현장 실무자 기준 정보량 축소 운영계획 (즉시 적용)

### 13-1. 핵심 운영원칙
1. 한 화면에서 한 가지 판단만 하게 만든다. (위험 판단 / 즉시 조치 / 보고 완료)
2. 상단 1스크린 안에 `위험등급 + 지금 할 일 3개 + 담당/기한`만 우선 노출한다.
3. 설명형 문장은 기본 숨김 처리하고, 체크리스트형 행동 문구만 기본 노출한다.
4. 상세 데이터(근거/이력/추세)는 접기 기본값으로 제공한다.

### 13-2. 정보 우선순위 분류 (P0/P1/P2)
- P0 (즉시행동): 위험등급, 우선조치 3개, 담당자, 기한, 보고/확인 버튼
- P1 (판단보조): 점수 근거 요약 1~2줄, 전일 대비 변화, 경향 아이콘
- P2 (기록/분석): 상세 로그, 긴 해설, 과거 이력 전체, 고급 통계

원칙: 모바일 기본 화면에는 P0만 완전 노출, P1은 요약 노출, P2는 접힘/별도 진입.

### 13-3. 화면별 기본 노출 규칙
- 홈
  - 기본 노출: 오늘 위험등급, 오늘 우선조치 3개, 시작 버튼 1개
  - 접힘 처리: 부연 설명, 과거 추이 상세, 장문 인사이트
- OCR 분석
  - 기본 노출: 업로드/촬영 CTA, 분석 시작 버튼, 진행상태
  - 접힘 처리: 엔진 설명, 기술 로그, 세부 파라미터
- AI 리스크
  - 기본 노출: 위험버킷(R/Y/G), 즉시조치, 담당/기한
  - 접힘 처리: 상세 채점근거, 확장 해설, 이력 전체

### 13-4. 문구 압축 기준 (실무자용)
- 금지: 기능명 중심 문장 (예: "AI 리스크 분석 기능으로 진입")
- 권장: 행동 중심 문장 (예: "위험 확인", "즉시 조치", "보고 완료")
- 길이 기준:
  - 카드 제목 16자 이내
  - 본문 요약 28자 이내
  - 버튼 라벨 8자 이내

---

## 14) 2주 실행 플랜 (현장 테스트 포함)

### Week 1 (구조 고정)
1. 화면 요소 전수 분류: P0/P1/P2 태깅
2. 상단 요약 카드 고정: 위험등급/우선조치/담당/기한
3. 장문 영역 접힘 기본값 적용
4. CTA 단일화: 화면당 우선 버튼 1개

### Week 2 (검증/보정)
1. 현장 사용자 5명 태스크 테스트
2. 첫 판단까지 소요시간 측정
3. 오터치/재탐색 구간 로그 확인
4. 상위 3개 혼동 포인트 문구/배치 수정

---

## 15) 검증 KPI (실무자 체감 기준)

- 첫 판단 완료 시간: 30초 이내
- 첫 화면 스크롤 깊이: 기존 대비 40% 감소
- 즉시조치 버튼 도달률: 90% 이상
- 잘못된 경로 진입률: 20% 이상 감소
- 사용자 피드백 "정보가 많아 헷갈림": 50% 이상 감소

---

## 16) 운영 책임 분담

- 제품/기획: P0/P1/P2 우선순위 확정, 삭제/접힘 정책 승인
- 디자인: 1스크린 우선 레이아웃, 카드 밀도/타이포 축소
- 개발: 접힘 기본값, CTA 우선순위, 측정 이벤트 삽입
- 현장 리더: 테스트 참여자 모집, 실제 업무 시나리오 검증

운영 원칙: 기능 추가보다 정보 감량을 우선한다.

---

## 17) 실행 상태 업데이트 (2026-05-04)

### 17-1. 코드 반영 완료 — Phase 1 (밀도 축소)
- Dashboard: 모바일(`viewport < 640`)에서 `essential` 모드 자동 강제 적용, 모바일 수동 모드 버튼 비노출
- OCR: 모바일에서 소개/보조 정보 기본 축소, `운영·백업` 도구 패널 접힘 기본값 적용
- AI Risk(Predictive): 모바일에서 심화 패널(공종별 조치율, 교육 제안) 기본 접힘 적용

### 17-2. 코드 반영 완료 — Phase 2 (연결 흐름)
- OCR 분석 완료(results > 0) 후 모바일 고정 CTA "AI 리스크 분석 결과 보기 →" 자동 표시
- `onNavigateToPredictive` prop을 App.tsx → OcrAnalysis로 전달, 탭 전환 연결
- Dashboard → OCR: 모바일 하단 "시작하기" 고정 버튼(`setCurrentPage('ocr-analysis')`)으로 문구 단일화
- Dashboard → AI 리스크: quickActions `predictive-analysis` 탭 기존 확인

### 17-2.1 코드 반영 완료 — Phase 2.5 (터치 타겟)
- AI Risk(Predictive): 모바일 핵심 액션 버튼 3종(`경향 분석`/`개별 점검 영역`/`AI 인사이트`) `min-h-[44px]` 적용
- AI Risk(Predictive): 모바일 `온톨로지 맵 보기/숨기기` 토글 `min-h-[44px]` 적용
- OCR: 분석 후 보조 CTA `닫기` 버튼 `min-h-[44px]` 적용

### 17-3. 검증 결과
- 빌드 통과 (`built in 7.56s`)
- QA 증적 16건 캡처 완료 (`artifacts/mobile-qa/2026-05-04/`)
- `check:mobile-qa:evidence` → `READY_FOR_FINALIZATION` (16/16)
- `qa:mobile:finalize` → `RESULT=FINALIZED_PASS` ✅

### 17-3.1 코드 반영 완료 — Phase 3 Kickoff (PC Dashboard)
- Dashboard 데스크톱(`viewport >= 1024`, `!isEssentialMode`)에 `PC 운영 콘솔` 바로가기 패널 추가
- 바로가기 5종 연결: `OCR 운영` / `AI 리스크 운영` / `리포트` / `근로자 관리` / `설정/관리자`
- 목적: 모바일과 다른 운영 콘솔 역할 분리, 대량 처리 화면으로 1탭 전환 지원
- 빌드 재검증 통과 (`built in 5.84s`)

### 17-3.2 코드 반영 완료 — Phase 3 Kickoff (PC OCR 운영)
- OCR 데스크톱(`!isCompactMobile`)에 `PC 운영 배치 바로가기` 패널 추가
- 바로가기 5종 연결: `실패 우선 정렬` / `재분석 상세 열기` / `실패 큐 빠른조치` / `사유 QA 패널` / `관리자 활동 패널`
- 목적: 실패 큐/재분석/관리자 점검 흐름을 단일 구간에서 즉시 전환해 대량 처리 속도 개선
- 빌드 재검증 통과 (`built in 5.63s`)

### 17-3.3 코드 반영 완료 — Phase 3 Kickoff (PC AI Risk 운영)
- PredictiveAnalysis 데스크톱(`viewport >= 1024`)에 `PC 운영 바로가기` 패널 추가
- 바로가기 4종 연결: `버킷 중심 조치 보기` / `리스크 인사이트 펼치기` / `실행 계획 전체 보기` / `회의 리포트 인쇄`
- 목적: AI Risk 운영 판단을 요약 카드에서 바로 실행 패널로 전환해 회의·운영 흐름 단축
- 빌드 재검증 통과 (`built in 6.08s`)

### 17-3.4 코드 반영 완료 — Phase 3 Kickoff (PC Reports 운영)
- Reports 데스크톱에 `PC 운영 바로가기` 패널 추가
- 바로가기 5종 연결: `일괄 생성 시작` / `증빙 ZIP 내보내기` / `CSV 내보내기` / `근로자 상세 미리보기` / `회의 리포트 인쇄`
- 목적: 보고서 생성·증빙 내보내기·검토 동선을 한 구간에 모아 운영 전환 시간 단축

### 17-3.5 코드 반영 완료 — Phase 3 Kickoff (PC Settings 운영)
- Settings 데스크톱에 `PC 운영 바로가기` 패널 추가
- 바로가기 5종 연결: `테마 자동 모드` / `UI 지표 새로고침` / `헬스체크 실행` / `워크플로 진단 실행` / `설정 저장/적용`
- 목적: 설정·진단·저장 액션을 상단에서 즉시 실행하도록 정리해 운영 점검 반복 시간 단축
- 빌드 재검증 통과 (`built in 9.70s`)

### 17-3.6 사용성 보정 완료 — Reports/Settings PC 패널
- Reports: 바로가기 패널에 실행 가이드 문구 추가, 생성/내보내기 버튼 비활성 조건 연동, `근로자 상세 미리보기` 진입 시 `setPreviewIndex(0)` 고정
- Settings: 바로가기 패널에 실행 가이드 문구/`초보자 가이드 열기` 추가, `워크플로 진단 실행` 버튼에 로딩·대상 없음 비활성 조건 연동
- 빌드 재검증 통과 (`built in 5.23s`)

### 17-3.7 계측 반영 완료 — PC 운영 바로가기 패널 사용 이벤트
- PredictiveAnalysis/Reports/Settings PC 운영 바로가기 버튼 클릭 시 `trackUIViewMetric('cta_click', ...)` 기반 이벤트 기록 추가
- 페이지별 세션 ID(`createMetricSessionId`)를 분리해 액션 키(`actionKey`) 단위로 클릭 도달 로그 적재
- 패널 구분값(`panel: 'pc_quick_actions'`)을 payload에 포함해 기존 UI 모드 지표와 동일 저장소(`psi_view_mode_metrics`)에서 교차 확인 가능
- 빌드 재검증 통과 (`built in 5.80s`)

### 17-3.8 계측 가시화 완료 — Settings KPI Top 5
- Settings `UI 모드 실험 KPI 요약`에 `PC 운영 바로가기 클릭 Top 5` 패널 추가
- `event=cta_click` + `payload.panel=pc_quick_actions` 기준으로 페이지·액션키별 집계(총 클릭/액션 종류/최근 시각 포함) 노출
- 운영자가 즉시 상위 액션 도달 패턴을 확인하고 다음 UX 보정 우선순위를 결정할 수 있도록 구성
- 빌드 재검증 통과 (`built in 6.20s`)

### 17-3.9 하위 액션 미세조정 완료 — 라벨/배치 1차
- 저빈도 가정 3액션 1차 보정: Predictive `print_meeting_report` 라벨을 `회의용 리포트 인쇄`로 명확화, Reports `open_worker_preview` 라벨을 `근로자 1건 미리보기`로 구체화, Settings `open_beginner_guide`를 패널 후순위로 이동하고 `신규 사용자 가이드`로 변경
- 위 3액션 클릭에 `uiVariant: 'v2-lowfreq-tuning-1'` payload를 추가해 기존 로그 대비 추이 비교 가능 상태로 전환
- Settings KPI Top 5 패널에 `미세조정(v2) 비교 대상 클릭` 카운트 행을 추가해 운영 화면에서 즉시 비교 확인 가능
- 빌드 재검증 통과 (`built in 6.04s`)

### 17-3.10 추이 자동화 완료 — v2 일간 비교
- Settings KPI에 `uiVariant=v2-lowfreq-tuning-1` 기준 `오늘/어제/증감(건·%)` 자동 계산 행 추가
- 일간 비교는 로컬 날짜 기준으로 계산해 운영자가 별도 추출 없이 즉시 개선 추이를 확인할 수 있도록 구성
- 빌드 재검증 통과 (`built in 6.70s`)

### 17-3.11 2차 보정 완료 — 가이드 액션 v3 타겟 실험
- 보정 대상 액션을 `open_beginner_guide`로 고정하고, PC 운영 바로가기 내 위치를 선행 배치(첫 버튼)로 조정
- 버튼 라벨을 `빠른 시작 가이드`로 변경하고 계측 태그를 `uiVariant='v3-targeted-tuning-1'`로 분리
- Settings KPI에 가이드 액션 전용 비교 행(v2 누적/오늘 vs v3 누적/오늘)을 추가해 2차 보정 효과를 즉시 확인 가능하도록 구성
- 빌드 재검증 통과 (`built in 6.19s`)

### 17-3.12 카피 A/B 실험 활성화 — v3 가이드 CTA
- `open_beginner_guide` v3 버튼에 세션 해시 기반 A/B 카피 분기 적용 (`A: 빠른 시작 가이드`, `B: 처음 사용 안내`)
- 클릭 이벤트 payload에 `copyVariant`/`copyLabel`을 함께 기록해 v3 내부 카피별 성과 비교 가능 상태로 전환
- Settings KPI에 `v3 카피 A/B` 요약 행(누적 클릭 + 현재 세션 노출안) 추가
- 빌드 재검증 통과 (`built in 5.42s`)

### 17-3.13 승자 자동 고정 구현 — 1일 관찰 기준
- `open_beginner_guide` v3 카피 A/B의 최근 24시간 표본을 기준으로 승자 자동 고정 로직 추가
- 자동 고정 조건: 최근 24h 표본 8건 이상, A/B 격차 2건 이상일 때 승자 고정(`psi_settings_guide_copy_winner_v1`)
- Settings KPI에 승자 고정 상태(또는 관찰 중 표본 현황) 표시를 추가해 운영자가 현재 판단 상태를 즉시 확인 가능
- 빌드 재검증 통과 (`built in 5.33s`)

### 17-4.1 배포 체크 보강 — `check:mobile-qa:evidence` 실패 원인 정리
- 스크립트(`scripts/check-mobile-qa-evidence.cjs`)는 `artifacts/mobile-qa/2026-05-04` 경로의 16개 PNG 중 누락이 1개라도 있으면 `RESULT=NOT_READY`와 함께 Exit 1 반환
- 따라서 실패 원인은 코드 빌드 이슈가 아니라 `증적 파일 누락(미캡처/경로 불일치/작업 디렉터리 불일치)` 분기이며, 재현 시 `TOTAL_MISSING` 값으로 즉시 확인 가능
- 현재 워크스페이스 재실행 결과는 `TOTAL_MISSING=0`, `RESULT=READY_FOR_FINALIZATION` 확인

### 17-4.2 초기 로딩 오류화면 깜빡임 개선
- 원인: `index.tsx`의 전역 `error`/`unhandledrejection` 핸들러가 복구 가능한 초기 예외에도 즉시 치명오류 화면을 덮어써 일시 노출 후 정상 복구되는 현상 발생
- 개선: 앱 마운트 여부 확인(`data-psi-mounted`) + 지연 렌더(900ms) 방식으로 치명오류 화면 노출을 보수화하고, 마운트 완료 시 대기 타이머 즉시 해제
- 효과: 복구 가능한 부트스트랩 예외에서 오류 화면 플래시를 억제하고 실제 치명 오류만 노출
- 빌드 재검증 통과 (`built in 5.40s`)

### 17-4.3 미구현 1순위 반영 — 모바일 헤더 높이/간격 공통화
- Dashboard/OcrAnalysis/PredictiveAnalysis 상단 히어로 영역의 모바일 기준 간격을 `rounded-2xl + p-4` 스케일로 공통화
- OcrAnalysis/PredictiveAnalysis 헤더 row gap을 `gap-3 sm:gap-4`로 정렬해 첫 화면 높이 체감을 통일
- 체크리스트 `1) 공통 구현 체크`의 `모바일 헤더 높이/간격 공통화` 항목 [x] 반영
- 검증: `npm run build` PASS (`built in 5.16s`), `check:mobile-qa:evidence` → `READY_FOR_FINALIZATION`

### 17-4.4 미구현 2순위 반영 — CTA 버튼 스타일 공통화
- Dashboard/OCR/Predictive 핵심 모바일 CTA를 동일 스케일(`min-h-[48px]`, `rounded-2xl`, `px-4 py-3`, `text-sm font-black`)로 정렬
- 주요 CTA에 `transition-colors` + `active:scale-[0.99]`를 공통 적용해 탭 피드백 체감을 일관화
- 적용 범위: Dashboard `시작하기`, OCR `분석 시작`/`AI 리스크 분석 결과 보기`, Predictive 모바일 토글 CTA 2종
- 검증: `npm run build` PASS (`built in 5.24s`), `check:mobile-qa:evidence` → `READY_FOR_FINALIZATION`

### 17-4.5 미구현 1순위 반영 — 카드 토큰 공통화
- `components/shared/cardTokens.ts`를 추가해 공통 카드 기준(`rounded-2xl`, border, shadow, padding scale)을 토큰으로 정리
- `InterpretationCardGrid` / `SummaryMetricGrid` / `ControlPanelCard` / `toneVariants`에 공통 토큰을 연결해 Dashboard/OCR/Predictive 카드 스케일을 일치시킴
- 목적: 핵심 3화면의 카드 반경/패딩/그림자 기준을 분산 문자열이 아닌 shared token으로 통합해 후속 보정 비용 축소
- 검증: VS Code Problems 기준 변경 파일 오류 없음 + `npm run build` PASS(`built in 6.57s`)

### 17-4.6 미구현 2·3순위 반영 — 상태 문구 분리 + AI Risk 상태 제어
- OCR/Predictive의 빈 상태 문구를 `대상 없음` / `데이터 부족` / `이력 없음` / `동기화 실패`로 분리해 사용자가 다음 행동을 즉시 이해하도록 정리
- Predictive 상단 요약 카드에 `AI 리스크 재계산` / `상태 동기화 재시도` 패널을 추가하고 `idle/loading/success/error` 상태 배지 및 오류 문구를 연결
- 재계산 버튼은 데이터 존재 + 비동기 동기화 중이 아닐 때만 활성화되며, 재시도 버튼은 관리자 인증 + 동기화 실패 상태에서만 활성화되도록 분기
- 검증: VS Code Problems 기준 변경 파일 오류 없음 + `check:mobile-qa:evidence` `READY_FOR_FINALIZATION` + `qa:mobile:finalize` `FINALIZED_PASS`

### 17-4. 다음 후속 액션 (Phase 2.5 — Home 구조 정밀화)
1. ✅ 모바일 홈 KPI 카드 3개 + 위험도 요약 1차 노출 구조 유지(추가 중복 카드 모바일 숨김 반영)
2. ✅ "시작하기" CTA 모바일 단일 고정 반영
3. ✅ 최근 리포트 3건 이내 모바일 제한: Dashboard 모바일 Essential 구간에 최근 리포트 카드(최신 3건) 추가
4. ✅ 공통 터치 타겟 44x44(핵심 모바일 액션 버튼 범위) 반영
5. ✅ 카드 반경/패딩/그림자 토큰 통일(Dashboard/OCR/Predictive 공통 카드 스케일 정렬)
6. ✅ OCR/AI Risk 결과 없음·지연·오류 문구 분리 정비
7. ✅ AI Risk 재계산/재시도 버튼 조건 분기 정식화
8. ✅ `npm run build` + `check:mobile-qa:evidence` 재검증 및 320/360/375/390 실측 QA 델타 없음 확인
9. 🔜 다음 1순위: `17) 실행 상태 업데이트`와 QA 최종화 템플릿에 오늘 검증 스냅샷 3줄(완료/다음/검증) 기록

---

## 18) 현재사항 종료 확인 + 다음사항 즉시 착수 기록

### 18-1. 현재사항 종료 확인(체크 3개)
- [x] `npm run build` PASS
- [x] `npm run check:mobile-qa:evidence` → `READY_FOR_FINALIZATION` (16/16)
- [x] `npm run qa:mobile:finalize` → `FINALIZED_PASS`

### 18-2. 다음사항(바로 실행 1건)
1. `17) 실행 상태 업데이트`에 오늘 검증 결과(`build 6.57s`, `READY_FOR_FINALIZATION`, `FINALIZED_PASS`) 3줄 델타 기록
2. `MOBILE_3SCREEN_VIEWPORT_QA_FINALIZATION_TEMPLATE_2026-05-04.md`에 오늘 실행 시각 기준 점검 메모 1줄 추가
3. 다음 세션 시작 작업을 `QA 보고서/증적 리포트 갱신 여부 점검`으로 고정

### 18-3. 세션 종료 시 3줄 기록 규칙
- 완료: 오늘 끝낸 항목 1줄
- 다음: 다음 세션 첫 작업 1줄
- 검증: PASS 명령 1줄

