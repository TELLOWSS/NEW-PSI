# NEW-PSI Codex 전수검사 상태

최종 갱신: 2026-06-19 15:50 KST

## 검사 원칙

- 원본 JSON은 읽기 전용으로 검사하며 수정·삭제·대체하지 않는다.
- 모든 레코드를 스트리밍 검사기로 통과시킨다.
- 원본 JSON, 이미지, 개인정보, 전체 DOM·로그·diff는 대화에 출력하지 않는다.
- 상세 증거는 `artifacts/audit/`에 저장하고 주 스레드에는 통계·핵심 오류·경로만 보고한다.
- 계산 규칙 변경은 데이터와 코드의 근거가 확인된 뒤에만 수행한다.

## 1단계 — 현재 상태 확인 완료

- 저장소: `NEW-PSI-github`
- 브랜치: `main`
- 원격: `origin https://github.com/TELLOWSS/NEW-PSI.git`
- 기준 커밋: `2b411a1` (`allow baseline setup before survey data`)
- 데이터 후보: `PSI_Backup`, `psi_ocr_records` 계열 25파일
- 파일 크기: 약 82MB~1.09GB
- 주요 검증 명령:
  - `npm test`
  - `npm run verify:fast`
  - `npm run verify:release`
  - `npm run build`
  - `npm run audit:data`

## 2단계 — 전수검사 프로그램 작성 완료

- 검사기: `scripts/audit-data.mjs`
- 처리 방식: 전체 JSON을 한꺼번에 메모리에 올리지 않고 레코드 단위 스트리밍
- 개인정보 보호: CSV에는 원문 식별자가 아니라 잘린 SHA-256 해시만 기록
- 검사 범위:
  - 전체 건수, 파일 해시, 파일 내부·파일 간 중복
  - 필수값, 날짜, 숫자 범위, enum, 승인·워크플로 참조
  - 근로자 식별키·UUID 연결
  - 점수와 6대 지표, 저장 등급과 기본 임계값의 불일치
  - 백업 세대 간 추가·삭제·변경·동일 레코드
- 검사기 자체 검증:
  - 동일 파일명의 저점수 건수가 누적되던 결함 수정
  - 파일 경로를 고유하게 기록하도록 수정

## 3단계 — 전체 데이터 산출물 생성 완료

- 총 파일: 25
- 총 용량: 6,932,913,173바이트
- 전수검사 레코드: 2,752
- 파일 처리 실패: 0
- 오류: 1,111
- 경고: 450
- 지표 불일치: 815
- 완전 동일 파일 그룹: 1
- 동일 백업 중복:
  - 2026-01-22 월간/주간 파일 2개
  - 각 192건, SHA-256 완전 동일
- 주요 데이터 증거:
  - 2026-01-22 백업 192건이 근로자 관리 단위 30개로 축소됨
  - 두 중복 파일 각각 저점수 167건
  - 2026-04-02 백업 218건 중 미래일자 1건(2026-12-25)
  - 2026-04-26 백업 217건은 모두 2026-03-26 한 날짜에 집중
  - 비식별 공개 요약의 579건과 정확히 일치하는 원본 스냅샷은 발견되지 않음
- 산출물:
  - `artifacts/audit/summary.json`
  - `artifacts/audit/errors.csv`
  - `artifacts/audit/warnings.csv`
  - `artifacts/audit/restore-diff.json`
  - `artifacts/audit/metric-mismatches.csv`

## 4단계 — 전문영역 검증 완료

- [x] A. 데이터 무결성
- [x] B. 지표 계산 규칙
- [x] C. 브라우저 화면 숫자
- [x] D. 관리자 조치·리포트·교육 연결
- [x] E. React·TypeScript·API·DB 품질
- [x] F. 프로덕션 빌드와 Vercel 운영 검증

확정된 출시 차단 후보:

1. 동명이인·동일 공종 오병합과 기존 UUID 덮어쓰기
2. 저장 등급과 화면별 재계산 등급 불일치
3. 기간·팀 필터가 일부 숫자와 대상 선택에 적용되지 않음
4. 관리자 조치→리포트→교육→서명→재평가를 묶는 사건 ID와 감사 폐루프 부재
5. 교육 이수율 분모가 실제 지정 대상자 전체가 아님
6. 인증 없는 교육 서명 생성 경로와 위조 가능한 토큰 비밀키 폴백
7. 소프트 삭제 스키마 누락 시 영구삭제로 전환
8. 서버 저장 실패를 로컬 성공으로 숨기는 경로

## 5단계 — 종합 및 P0 개선 완료

완료한 핵심 개선:

- 입력 UUID 보존, 안정 UUID 충돌 병합 금지, 레거시 전용 fallback
- 동일 근로자+평가일 대량 충돌, 미래일자, 잘못된 달력 날짜 복원 차단
- IndexedDB 일괄 복원의 단일 트랜잭션 처리
- 성과 기간 필터, 불안전행동 필터 선택, 빈 운영 대상, 안전점검 날짜 수정
- 교육 대상자 안정 ID 전송 및 지정 대상자 기반 이수율
- 인증 없는 교육 서명 차단, 안전한 제출 API 연결
- 관리자 대리확약 구분, 비밀키 실패-차단
- 소프트 삭제 스키마 누락 시 영구삭제 금지
- React key 경고 제거, Chart.js 외부 CDN 의존 제거

최종 검증:

- 전체 테스트: 18파일 / 104개 통과
- TypeScript: 통과
- `verify:release`: 통과
- 브라우저 핵심 흐름: 10/10 통과
- 모바일 390px 가로 넘침: 없음
- 프로덕션 의존성 취약점: 0건
- 개발 의존성: 21건 잔존, Vercel CLI·Vitest 메이저 업그레이드 필요

Vercel:

- 최신 프로덕션 배포: `READY`
- 시각: 2026-06-18 16:40:28 KST
- 배포 커밋: `2b411a1`
- 최근 24시간 오류·경고 런타임 로그: 0건
- 개선 커밋 `0b343bd`를 2026-06-19 GitHub `main`과 Vercel 프로덕션에 배포 완료

남은 출시 전 과제:

1. `case_id` 기반 관리자 조치→리포트→교육→서명→재평가 폐루프
2. 관리자 RBAC, 현장·테넌트 격리, Supabase RLS 강화
3. 지표 규칙 버전과 화면 전체의 단일 계산 기준
4. 대용량 서버 스트리밍 복원·중단 재개·롤백
5. Vercel CLI 54 / Vitest 4 업그레이드와 개발 의존성 0건
6. 모바일 QA 증거 16/16, 보안 헤더, 관측성·백업 복구 훈련

종합 보고서:

- `artifacts/audit/product-readiness.md`
- `artifacts/audit/browser/browser-verification.json`

## 2026-06-19 관리자 기준 위험도 등록 개선

- 기본 사이드바에서 `근로자 의견 분석`을 노출하고 기존 UI 구성 설정을 버전 3으로 이전했다.
- 관리자 기준은 근로자 Q3 응답과 분리한 채 피해 규모·노출 빈도·방호 상태 3문항으로 권고한다.
- 월별 등록 진행률, 미등록 공종 연속 처리, 전월 기준의 미등록 항목만 복사하는 기능을 추가했다.
- 공종 카드에는 근로자 응답 분포와 주요 취약점을 참고자료로만 표시한다.
- 전월 기준 일괄 저장 API를 추가하고 100건 초과·월/공종 중복을 차단했다.
- 대용량 월 데이터는 공종별로 한 번만 분류해 카드별 반복 검색을 줄였다.
- 전체 검사: 20개 파일 / 110개 테스트 통과.
- `verify:release`: 통과, 프로덕션 빌드 903개 모듈 통과.
- 브라우저 검증: 7/7 통과, 390px 가로 넘침 및 페이지 오류 없음.
- 상세 결과: `artifacts/audit/manager-risk-baseline-review.md`
- 브라우저 증거: `artifacts/audit/browser/survey-risk-baseline-verification.json`

## 2026-06-19 관리자 기준 판단 근거·변경 이력

- 1차 개선분 커밋 `a908ba1`을 GitHub `main`에 푸시했다.
- Vercel 프로덕션 배포 `dpl_HJSJnp72SLyN81UKnZ1cvVN1hUJF`가 `READY` 상태이며 커밋 `a908ba1`과 일치한다.
- 빠른 판정 3문항, 현장 근거 메모, 입력 방식, 규칙 버전, 작성자, 변경 전후 등급을 보존하도록 확장했다.
- 직접 선택·빠른 판정·전월 복사를 구분하고 관리자 화면에 최근 변경 이력을 표시한다.
- 기존 기준 테이블을 변경하지 않고 별도 `survey_risk_baseline_history` 테이블을 추가하는 무중단 마이그레이션을 작성했다.
- 신규 이력 테이블은 RLS를 켜고 `anon`·`authenticated` 접근을 철회하며 서버 `service_role`만 조회·입력한다.
- Supabase 접속 자격이 없는 환경에서는 기존 팀 기준 저장을 유지하고 변경 이력은 브라우저 로컬에 최대 500건 보존한다.
- 전체 검사: 21개 파일 / 112개 테스트 통과.
- `verify:release`: 통과, 프로덕션 빌드 903개 모듈 통과.
- 브라우저 검증: 8/8 통과, 판단 근거 저장 요청·변경 이력 화면·390px 모바일·오류 0건 확인.
- DB 마이그레이션: `supabase_survey_risk_baseline_history_migration.sql`
- 상세 결과: `artifacts/audit/manager-risk-baseline-history-review.md`

## 2026-06-19 GitHub·Vercel 동기화 및 개발 공급망 정리

- 관리자 기준 판단 근거·변경 이력 커밋 `50262be`를 GitHub `main`에 푸시했다.
- Vercel 프로덕션 배포 `dpl_37A76kKehSjAHkh39AEV7sEZW4rK`가 `READY`이며 커밋 `50262be`와 일치한다.
- 공개 프로덕션 주소가 HTTP 200으로 응답하며 최근 30분 오류·치명·경고 런타임 로그는 0건이다.
- 운영 의존성 감사 결과는 취약점 0건이다.
- Vitest를 4.1.9로 올려 기존 치명 개발 취약점을 제거했다.
- Vercel Git 배포에서 사용하지 않는 로컬 Vercel CLI 의존성을 제거했다.
- 로컬 API 개발은 `scripts/run-vercel-dev.cjs`가 전역 Vercel CLI를 확인한 뒤 실행한다.
- 전체 운영·개발 의존성 감사 결과는 취약점 0건이다.
- 업그레이드 후 21개 테스트 파일 / 112개 테스트와 TypeScript 검사를 재통과했다.

## 2026-06-19 상품화 백로그·보안 헤더·모바일 증거 개선

- 미적용 Supabase 이력 테이블을 `docs/PRODUCTIZATION_BACKLOG.md`에 외부 권한 대기 항목으로 고정했다.
- 적용 순서, RLS·권한 검증, API 확인, 완료 판정 SQL을 기록해 다음 작업에서 즉시 재개할 수 있다.
- 상품화 상태 자동점검 `scripts/audit-productization.mjs`를 추가했다.
- 현재 자동 판정은 완료 3, 부분완료 1, 미시작 4, 외부권한 대기 1이다.
- P1 미진행은 `case_id` 폐루프, RBAC·현장/테넌트 RLS, 대용량 스트리밍 복원이며 지표 단일 기준은 부분완료다.
- Vercel 공통 응답에 CSP, 클릭재킹 방지, MIME 스니핑 방지, Referrer, Permissions, COOP, CORP 7개 보안 헤더를 추가했다.
- 보안 헤더 정적 검사와 Vitest 회귀검사를 릴리스 검사에 포함했다.
- 모바일 QA 자동화의 오래된 메뉴 선택자와 Windows 프리뷰 종료 문제를 수정했다.
- 320·360·375·390px에서 탐색·대시보드·OCR·선행 위험신호 16개 증거를 2026-06-19 기준으로 재생성했다.
- 모바일 증거 자동검사 결과 16/16, 누락 0건이다.
- 선행 위험신호 화면의 오래된 `모바일 검증 2026-05-04` 배지를 `2026-06-19`로 수정했다.
- 상품화 자동점검: `artifacts/audit/productization-status.json`, `artifacts/audit/productization-status.md`
- 모바일 증거: `artifacts/mobile-qa/2026-06-19/`
- GitHub `main` 커밋 `e1e6b13`과 Vercel 프로덕션 배포 `dpl_9tuUwbhJW1bGFrZPWiXigzJNrKgp`가 연결됐다.
- 프로덕션 보안 헤더 실제 응답 검사 7/7, HTTP 200, 최근 30분 오류·경고·치명 로그 0건이다.
- CSP 적용 상태에서 관리자 기준 위험도 브라우저 흐름 8/8을 재검증했다.
- 프로덕션 브라우저 검증 중 관리자 세션 표시가 없는 테스트 격리 문제를 발견해, 서버 인증과 클라이언트 세션 경계를 모두 모의하도록 검증 스크립트를 보완했다.

## 2026-06-20 관리자 기준 간편 등록·체감 비교 의사결정 UX

- 전체 월 상태에서 `이번 달 기준 등록 시작` 버튼으로 현재 월과 우선 공종을 한 번에 선택하도록 개선했다.
- 우선 공종은 미등록 여부, 해당 월 근로자 응답 수, 공종명 순으로 결정해 데이터가 많은 미등록 공종부터 처리한다.
- 공종별로 추락·붕괴·인양·비래·분진 등 현장 확인 힌트를 표시하되, 힌트와 근로자 응답은 관리자 권고 점수 계산에 사용하지 않는다.
- 피해 규모·노출 빈도·방호 상태 3문항의 권고가 완성된 뒤에만 관리자 기준과 근로자 체감의 예상 차이를 표시한다.
- 인식 차이 방향에 따라 TBM 강조, 작업조건·방호조치 재확인, 현행 조치 유지 안내를 제공한다.
- 미등록 공종을 근로자 응답이 많은 순으로 정렬하고 `미등록`·`전체` 보기 전환을 추가했다.
- 기존 `buildSurveyRiskGapRows`를 비교 미리보기에서도 그대로 사용해 화면 간 계산식을 새로 만들지 않았다.
- 단위·회귀검사: 22개 파일 / 117개 테스트 통과.
- `verify:release`: TypeScript, 보안 헤더 7/7, 점수 일관성 ±8, 프로덕션 빌드 903개 모듈 통과.
- 브라우저 전체 흐름: 11/11 통과, 저장 요청의 월·공종·등급·3문항·근거 메모 확인, 390px 가로 넘침과 실행 오류 0건.
- 시각 증거: `artifacts/audit/browser/survey-risk-baseline-wizard.png`, `survey-risk-baseline-mobile-decision.png`, `survey-risk-baseline-mobile-comparison.png`
- 상세 검토: `artifacts/audit/manager-risk-baseline-decision-ux-review.md`
- 상품화 자동판정: 완료 4, 부분완료 1, 미시작 4, 외부권한 대기 1.
- GitHub `main` 커밋 `4df1c52`와 Vercel 프로덕션 배포 `dpl_EwRV5tT54ogyuuD8qaP6G8udx78R`가 일치하며 `READY` 상태다.
- 프로덕션 주소 `https://new-psi-eta.vercel.app`에서 관리자 기준 흐름 11/11과 실제 보안 헤더 7/7을 재통과했다.
- 해당 배포의 최근 30분 오류·경고·치명 런타임 로그는 0건이다.

## 2026-06-20 현장 친화 언어 전수개선

- 상품화 자동판정은 완료 5, 부분완료 1, 미시작 4, 외부권한 대기 1이다.
- 주요 사용자 화면 13개 파일을 대상으로 개발자 언어 자동감사 도구를 추가했다.
- 최초 감사에서 즉시 개선 33건, 쉬운 설명 병기 권장 67건, 합계 100건을 발견했다.
- `백로그`, `팀 DB`, `브라우저 저장`, `BULK MODE`, `SUCCESS RATE`, `스키마`, `캐시`, `Preflight`, `API Cooling Down` 등 내부 표현을 검토 대기, 팀 공동 저장소, 이 기기 임시 저장, 여러 명 발송, 성공률, 저장 구조, 임시 보관, 사전 점검, 분석 요청 대기 등으로 바꿨다.
- API·OCR처럼 현장 지원과 장애 대응에 필요한 기술명은 쉬운 설명 뒤 괄호에만 남겼다.
- 언어 감사 결과는 즉시 개선 0건, 설명 병기 권장 0건이며 릴리스 검사에 포함했다.
- 전체 회귀검사는 23개 파일 / 119개 테스트, TypeScript, 점수 일관성 ±8, 보안 헤더 7/7, 운영·개발 의존성 취약점 0건을 통과했다.
- 프로덕션 빌드는 903개 모듈을 통과했다.
- 브라우저 검증은 핵심 6개 화면과 390px 모바일을 포함해 14/14 통과했으며, 가로 넘침·페이지 오류·콘솔 오류는 0건이다.
- 상세 언어 감사: `artifacts/audit/ui-language-audit.md`
- 화면 검증 결과: `artifacts/audit/browser/plain-language/plain-language-verification.json`
- 시각 증거: `artifacts/audit/browser/plain-language/`
- 다음 최우선 상품화 과제는 `case_id` 기반 위험 발견→조치→리포트→교육→서명→재평가 보호 폐루프다.
- 개선 커밋 `f09b06f`를 GitHub `main`에 푸시했다.
- Vercel 프로덕션 배포 `dpl_Adb58dskZpL92yxisGTekbimXtkE`가 같은 커밋으로 `READY` 상태가 됐다.
- 프로덕션 주소 `https://new-psi-eta.vercel.app`는 HTTP 200, 현장 언어 검증 14/14, 보안 헤더 7/7을 통과했다.
- 해당 배포의 최근 30분 오류·경고·치명 런타임 로그는 0건이다.

## 2026-06-20 근로자 리포트·안전역량인증서 상품화

- 사용자 제공 PDF `PSI_Report_가가다_형틀_20260620.pdf` 2쪽을 이미지로 렌더링해 글꼴, 모국어 혼입, 정보 구조를 확인했다.
- 기존 인증서의 명조 계열 강제 글꼴, 캄보디아어 안의 한국어 작업명·위험명·`[KO]` 혼입, 한국어 레이더 축, 월별 기록의 근로자 중복 표시를 확인했다.
- 한국어·베트남어·중국어·태국어·미얀마어·우즈베크어·크메르어·인도네시아어·말레이어·몽골어·러시아어·카자흐어·네팔어·영어 14개 언어의 단일 리포트 정책을 추가했다.
- Noto Sans 계열의 한글·중국어·태국어·크메르어·미얀마어·데바나가리 글꼴과 운영체제 대체 글꼴 순서를 지정했다.
- 근로자 전달본은 모국어 인증서 1장으로 분리하고, 관리자 분석본은 모국어 인증서와 한국어 검증 부록 2장으로 유지했다.
- 외국인 인증서 전면에서 한국어 병기와 한국어 레이더 축을 제거하고, 공종·등급·제목·안내·안전표지를 해당 언어로 표시한다.
- 안전역량인증서에 PSI 발행번호, 평가기간, 평가개월, 누적 평가기록, 등급 기준, 발행 범위와 법정 자격 대체 불가 안내를 추가했다.
- 개인별 안전보고서 목록은 월별 기록이 아니라 근로자 한 명당 한 줄로 묶고 최근 평가, 평가기간, 개월 수, 기록 수, 첫 평가 대비 점수 변화, 모국어 준비 상태를 표시한다.
- 모국어·글꼴 감사 14/14, 브라우저 전체 흐름 10/10, 실제 PDF 2쪽 생성, 390px 가로 넘침과 실행 오류 0건을 확인했다.
- 전체 회귀검사는 24개 파일 / 124개 테스트와 TypeScript 검사를 통과했다.
- 상품화 자동판정은 완료 6, 부분완료 1, 미시작 4, 외부권한 대기 1로 갱신됐다.
- 언어 감사: `artifacts/audit/report-language-audit.md`
- 언어 행렬: `artifacts/audit/report-language-matrix.csv`
- 브라우저·PDF 증거: `artifacts/audit/browser/worker-report/`

### 운영 배포 확인
- 개선 커밋 `d4ce34e`를 GitHub `main`에 동기화했다.
- Vercel 프로덕션 배포 `dpl_Fjy2VTSazkAHuraQcBreeWZ5cLUT`가 같은 커밋으로 `READY` 상태임을 확인했다.
- 운영 주소 `https://new-psi-eta.vercel.app`에서 HTTP 200을 확인했다.
- 운영 브라우저 근로자 리포트 검증 10/10, 현장 언어 검증 14/14, 보안 헤더 7/7을 통과했다.
- 해당 배포의 최근 30분 오류·경고·치명 런타임 로그는 0건이다.

## 2026-06-20 근로자 리포트 월별 중복 표시 회귀 수정

- 실제 첫 진입에서는 팀·공종 요약 탭이 열려 월별 원본 기록이 반복 노출되는 문제를 확인했다.
- 2026-06-17 비식별 요약은 579건을 394명으로 집계했으나, 2026-06-19 요약은 같은 579건을 579명으로 분리해 월별 식별번호 우선 정책의 회귀를 확인했다.
- 개인정보·등록 근로자 연결의 엄격한 UUID 기준은 유지하고, 리포트 누적 집계에서만 공종+이름+국적 기준을 우선 적용했다.
- 근로자 리포트(관리자 분석)의 기본 탭을 개인별 안전보고서로 변경했다.
- 목록 열을 평가기간·기록, 최근 점수, 점수 변화로 분리하고 `1건 미리보기`를 `통합 리포트 보기`로 변경했다.
- 월별 식별번호가 서로 다른 동일 근로자 3개월 데이터를 넣어 첫 진입부터 한 줄로 묶이는 브라우저 회귀검사를 추가했다.
- 전체 테스트 24개 파일 / 125개, TypeScript, 14개 언어, 프로덕션 빌드 905개 모듈을 통과했다.
- 브라우저 검증 11/11을 통과했으며 3개월·총 3건·2026.04–2026.06·첫 평가 대비 +13점을 한 행에서 확인했다.
- 상세 점검: `artifacts/audit/worker-report-grouping-regression.md`
- 화면 증거: `artifacts/audit/browser/worker-report/worker-target-list.png`
- 개선 커밋 `8156f46`을 GitHub `main`에 동기화했다.
- Vercel 프로덕션 배포 `dpl_3zk5gvJeyN7QpSGf2xab5gH3ATcD`가 같은 커밋으로 `READY` 상태가 됐다.
- 운영 주소 `https://new-psi-eta.vercel.app`에서 첫 진입·상이한 월별 식별번호·한 줄 통합·기간·기록 수·점수 변화 검증 11/11을 통과했다.
- 운영 보안 헤더 7/7을 통과했고 해당 배포의 최근 30분 오류·경고·치명 로그는 0건이다.

## 2026-06-22 보호사건 폐루프 1차 구현

- 예측 위험 발견부터 보호조치, 리포트, 교육, 근로자 본인 확인·서명, 후속 재평가까지 하나의 `case_id`로 연결하는 6단계 보호사건 모델을 추가했다.
- 보호조치 완료 전 리포트, 리포트 완료 전 교육처럼 앞 단계를 건너뛸 수 없도록 순서 검사를 적용했다.
- 관리자 대리서명은 근로자 이해 완료로 처리하지 않으며, 근로자가 직접 안내 확인·체크·서명을 완료한 경우에만 본인 확인 단계가 자동 완료된다.
- 재평가는 관리자 수동 확인만으로 닫지 않고, 본인 확인 이후 등록된 새 근로자 평가 기록이 연결될 때 자동 완료된다.
- 브라우저 로컬 저장과 관리자 API를 함께 사용하며, 서버 스키마 적용 전에는 로컬 흐름을 유지하도록 호환 모드를 적용했다.
- `safety_cases`, `safety_case_events`와 기존 실행계획·교육 테이블의 `case_id` 확장용 마이그레이션을 준비했다. 원격 Supabase에는 아직 임의 적용하지 않았다.
- 보호사건 단위 테스트 5/5, 교육 제출 안전성 포함 표적 테스트 12/12, TypeScript, TDZ, 화면 용어 0건, 리포트 언어 14/14를 통과했다.
- 다음 확인 단계는 전체 테스트, 프로덕션 빌드, 실제 브라우저 6단계 표시, GitHub·Vercel 동기화다.

### 최종 확인

- 전체 테스트 25개 파일 / 130개 통과.
- 출시 검사에서 TypeScript, TDZ, 보안 헤더 7/7, 점수 일관성 ±8, 화면 용어 0건, 리포트 언어 14/14, 프로덕션 빌드 907개 모듈을 통과했다.
- 보호사건 브라우저 검증 7/7을 통과했고 데스크톱·390px·교육 인계 화면 증거를 저장했다.
- 보호조치 완료와 전체 보호 완료 문구를 분리해 사건이 아직 진행 중인데 전체 완료로 오해하는 표현을 제거했다.
- 상품화 자동점검은 완료 6, 부분완료 2, 미시작 3, 외부권한 대기 1로 갱신했다.
- 상세 보고서: `artifacts/audit/safety-case-closed-loop-review.md`
- 브라우저 증거: `artifacts/audit/browser/safety-case/`
- 남은 외부 작업: `supabase_safety_case_closed_loop_migration.sql` 원격 적용과 실제 관리자·근로자 다기기 재접속 검증.
