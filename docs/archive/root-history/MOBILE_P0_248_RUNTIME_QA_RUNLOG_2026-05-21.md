# MOBILE P0(2/4/8) RUNTIME QA RUNLOG (2026-05-21)

- 대상 화면: 2) 경보 알림 / 4) 위험인지 진단 / 8) 개입 추천
- 기준 문서:
  - `MOBILE_MOCKUP_REALITY_AUDIT_2026-05-20.md`
  - `MOBILE_VIEWPORT_QA_CHECKLIST.md`
- 기준 뷰포트: 320x568 / 360x800 / 375x812 / 390x844
- 목적: P0 체감 개선(상단 고정 CTA + 단계/상태 가시화 + 즉시 동작) 런타임 확인

---

## 1) 사전검증 (코드/빌드)

### 코드 반영 확인
- [x] 2번(`pages/SiteIssueManagement.tsx`)
  - 모바일 핵심 경보 `sticky top-2` 고정 카드 존재
  - CTA 문구 `즉시 조치 시작` 반영
- [x] 4번(`pages/WorkerTraining.tsx`)
  - `4) 위험인지 진단 · 빠른 진행` 고정 카드 존재
  - `완료 단계/체크/다음 동작` 요약 표시
  - `handleNextAction` 즉시 이동 CTA 연결
- [x] 8번(`pages/InterventionCoaching.tsx`)
  - `8) 즉시조치 TOP1` 고정 카드 유지
  - 인계 데이터 없음(mock) 상태에서도 CTA 상태 전환 로직 존재
  - 완료 상태에서만 버튼 비활성 처리

### 빌드 검증
- [x] `npm run build` PASS (2026-05-21)

---

## 2) 런타임 수동 QA 체크리스트 (DevTools)

### 2-1) 3분 초단축 실행 순서
- 0:00~0:30: `npm run dev` 실행 후 `site-issue-management` 진입, 뷰포트 `320x568` 설정
- 0:30~1:20: 2번 화면에서 `즉시 조치 시작` 탭 → 상태 `조치 중` 반영 확인
- 1:20~2:10: `worker-training` 진입 → 상단 `빠른 진행` 카드의 `완료 단계/체크` 값 확인, CTA 탭으로 다음 섹션 이동 확인
- 2:10~2:50: `intervention-coaching` 진입 → TOP1 CTA 2회 탭, `미착수→진행중→완료` 전환 확인
- 2:50~3:00: 콘솔 에러 0건 확인 후 320 판정란(PASS/FAIL) 입력

### 공통 준비
- [ ] `npm run dev` 실행 후 앱 접속
- [ ] Device Toolbar: 320x568 / 360x800 / 375x812 / 390x844
- [ ] Zoom 100%, 세로 모드
- [ ] 콘솔 에러 0건

### 2번 경보 알림 (`site-issue-management`)
- [ ] 상단 고정 카드가 스크롤 중에도 시야 상단에 유지됨
- [ ] CTA `즉시 조치 시작` 탭 시 우선 이슈 상태가 `조치 중`으로 전환됨
- [ ] 상태 전환 후 배지/카운트(목록 및 요약)가 즉시 반영됨
- [ ] 320 폭에서 텍스트 잘림/버튼 겹침 없음

### 4번 위험인지 진단 (`worker-training`)
- [ ] 상단 고정 `빠른 진행` 카드가 스크롤 중 유지됨
- [ ] `완료 단계 n/4`, `체크 n/3`, `다음 동작`이 현재 입력 상태와 일치함
- [ ] CTA 탭 시 다음 필요 섹션으로 이동(언어→오디오→체크→서명/제출)
- [ ] 하단 고정 제출바와 상단 빠른 진행 카드가 충돌하지 않음

### 8번 개입 추천 (`intervention-coaching`)
- [ ] 상단 고정 `즉시조치 TOP1` 카드가 모바일에서 유지됨
- [ ] 인계 데이터 없음 상태에서 CTA 탭 시 `미착수 → 진행중 → 완료` 전환됨
- [ ] 완료된 항목 버튼은 비활성되고, 미완료 항목은 탭 가능 상태 유지
- [ ] 진행 상태(활성/완료 카운트)가 상태 전환과 동기화됨

---

## 3) 뷰포트별 판정 기록

### 320x568
- 판정: [ ] PASS  [ ] FAIL
- 메모:
  - 

### 360x800
- 판정: [ ] PASS  [ ] FAIL
- 메모:
  - 

### 375x812
- 판정: [ ] PASS  [ ] FAIL
- 메모:
  - 

### 390x844
- 판정: [ ] PASS  [ ] FAIL
- 메모:
  - 

---

## 4) 최종 판정
- 전체 판정: [ ] PASS  [ ] FAIL  [ ] 보류(수동 런타임 미실행)
- 블로커/이슈:
  - 
- 후속 조치:
  - 
