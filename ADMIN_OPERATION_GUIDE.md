# PSI 관리자 운영 매뉴얼 (모드 분리/링크 보안 기준)

## 1) 운영 모드 개요

- 관리자 모드
  - 전체 관리 기능 접근 가능
  - 수동 잠금 및 유휴 자동잠금(10분) 적용

- 근로자 모드
  - QR 링크로 진입
  - 서명 화면만 노출 (관리자 셸 비노출)
  - 단순화 UI 적용

## 2) 일일 운영 루틴 (권장)

1. 관리자 잠금 해제 후 `관리자 음성교육` 페이지 진입
2. 안내 문구 입력 및 언어 선택 (필요 시 말레이시아어 `ms-MY` 포함)
3. `생성` 클릭 → QR/공유 텍스트 발급
4. 필요 시 `풀스크린 표시`로 QR 가독성 확인 후 메신저로 공유 텍스트 전달
5. 만료 임박/만료 시 `링크 재발급`
6. 필요 시 테스트 세션 정리(삭제)

## 3) 링크 정책

- 링크는 `sessionId + exp + sig` 형태의 서명 링크로 발급됨
- 기본 만료: 12시간 (`TRAINING_LINK_TTL_MINUTES`로 조정 가능)
- 만료 시 근로자 페이지에서 자동 차단
- 관리자 페이지에서 재발급 가능

## 4) 재발급 운영 절차

1. `관리자 음성교육`에서 대상 세션 불러오기
2. QR 영역의 `링크 재발급` 클릭
3. 신규 URL/QR 공유
4. 하단 `링크 생성/재발급 이력`에서 시간/만료/세션 확인

## 4-1) QR 접속 불가 근로자 대응

1. `관리자 음성교육`의 QR 영역에서 하단 공유 텍스트를 복사
2. 근로자 개인 메신저(문자/카카오톡/WhatsApp 등)로 링크 직접 전달
3. 근로자 휴대폰 카메라 인식이 어려우면 관리자 휴대폰 브라우저에서 링크를 직접 열어 전달
4. 링크 만료 또는 접속 실패 시 `링크 재발급` 후 최신 링크만 다시 전달
5. 공용 기기 사용 시 제출 후 성공 화면을 관리자에게 확인시키고 브라우저를 종료

## 5) 근로자 제출 정책

- 유효 링크만 제출 허용
- 만료 링크 제출 차단
- 동일 세션 + 동일 이름 중복 제출 차단
- 제출 완료 후 근로자 화면 재제출 버튼 비활성

## 6) 현장 사고 예방 포인트

- 근로자에게는 관리자 링크 공유 금지
- QR은 작업구역별로 최신 링크만 노출
- 근무 교대 시 만료 여부 확인 후 필요 시 즉시 재발급
- 제출 오류 발생 시 이름 오탈자/중복 제출 여부 먼저 확인

## 7) 장애 대응 체크

- "유효하지 않은 링크"
  - URL에 `exp`, `sig` 존재 여부 확인
  - 재발급 후 최신 링크 재배포

- "링크 만료"
  - 관리자 화면에서 재발급 후 재전달

- "이미 제출된 이름"
  - 동일인 재제출로 판단
  - 관리자에서 세션/대상자 확인 후 필요 시 신규 세션 발급

## 8) 관리자 보안 운영 수칙

- 관리자 로그인 비밀번호는 서버 운영 설정값으로 관리 (`ADMIN_LOGIN_PASSWORD` 권장, 대체 `PSI_ADMIN_PASSWORD`)
- 관리자 세션 서명키는 `ADMIN_SESSION_SECRET`으로 관리하며 로그인 비밀번호와 분리
- 관리자 비밀번호/운영 비밀키는 주기적으로 변경
- 공용 PC 사용 시 업무 종료 즉시 `관리자 잠금`
- 브라우저 자동완성/비밀번호 저장 비활성 권장
- 실운영 도메인 외 테스트 링크 공유 금지

### 위험성평가 PC 월 마감 백업 운영 원칙

- 위험성평가 원문·수기답변·이미지·OCR 결과·검수 이력의 원본은 기기 IndexedDB와 PC 월별 JSON 파일에 보관합니다. 기록 저장·수정·로그인 시 서버에 자동 업로드하지 않습니다.
- OCR 분석실 `운영 · 백업`에서 대상 월을 선택하고 `PC 월 마감 백업`을 실행합니다. 전체 월 보기에서는 최신월만 백업합니다.
- 기본 파일명은 대상 월·세대 번호를 포함하며, 브라우저 다운로드 방식에는 짧은 내용 해시도 붙습니다. 전체 해시는 파일 내부 manifest에 항상 포함됩니다. 과거월 수정본은 기존 파일을 덮지 않고 다음 세대 파일로 보관합니다.
- 저장한 파일을 `백업 파일 검증·복원`으로 다시 선택해야 건수와 SHA-256 내용 해시를 검증합니다. 다운로드 요청만으로 마감 완료로 간주하지 않습니다.
- 검증 후 사용자가 복원을 확인할 때만 서버에 월별 해시 영수증과 근로자 ID별 고정 필드 요약을 등록합니다. 이름·본문·파일 원본·이미지는 보내지 않습니다.
- 대용량 복구는 원본 레코드의 해시를 먼저 검증하고, 브라우저에는 이미지를 제외한 기록만 복구합니다. 전체 원본은 PC 파일에 계속 남습니다.
- 같은 ID의 PC 기록이 더 최신이거나 수정 시각이 같거나 불명확하면 자동 덮어쓰지 않습니다.
- 오래된 월의 자동 삭제는 하지 않습니다. 검증된 월별 파일을 별도 저장장치에도 복사한 뒤 관리자 판단으로 정리합니다.
- 타회사에 전체 월별 원본 파일을 전달하지 않습니다. 근로자 이동은 안정 ID를 유지하되, 회사 간 이력 공개는 근로자 동의·권한 설계 없이 자동 실행하지 않습니다.
- 서버 영수증 및 기록 마스터 API는 관리자 세션과 서버 `service_role`만 허용합니다. 기존 서버 원문 행의 삭제·익명화는 별도 승인 후 진행합니다.

### 유료 OCR 승인 원칙

- 모든 문서는 무료 OCR 키로 먼저 실행하며 유료 키로 자동 전환하지 않음
- 무료 공급자 할당량이 소진된 경우에만 파일명·예상비용·최대비용을 표시한 승인창을 엶
- 관리자가 현재 접속 비밀번호를 다시 입력하고 `이 파일 1회 유료 실행 승인`을 눌러야 해당 파일 1건만 유료 실행 가능
- 입력한 비밀번호는 브라우저 저장소·URL·OCR 기록·감사 로그에 저장하지 않고 요청 직후 제거
- 비밀번호 불일치, 승인 만료/재사용, 유료 키 누락, 데이터베이스 승인 저장소 장애 시 유료 호출은 0회로 차단
- 다음 파일과 다음 작업은 다시 무료 키부터 시작하며 유료 승인을 자동 승계하지 않음

## 8-1) 관리자 비밀번호 분실/변경 절차

1. Vercel 프로젝트 환경변수에서 `ADMIN_LOGIN_PASSWORD` 값을 새 비밀번호로 변경
2. `ADMIN_SESSION_SECRET`이 비어 있지 않은지 함께 확인
3. 재배포 후 기존 브라우저 세션을 로그아웃하거나 새로고침
4. 새 비밀번호로 관리자 모드 진입 확인
5. 과거 하드코딩 비밀번호 방식은 사용하지 않음

## 9) 2026-03-24 운영 반영 사항

- 근로자 상세 리포트 운영 원칙
  - 외국인: 모국어 우선 표기 + 한국어 [KO] 병기
  - 한국인: 한국어 단일 표기
- 코칭/채점근거에서 모국어가 비어 있는 과거 데이터는 재분석으로 보완
- 외국인 리포트는 핵심 텍스트 가독성 확보를 위해 수기원본 영역을 기본 비노출

---

## 10) 2026-05-06 운영 검증 스냅샷

- 완료: 모바일 3코어(Home/OCR/AI Risk) 공통 카드 토큰 정렬, OCR/AI 상태 문구 분리, AI Risk 재계산/재시도 조건 분기 반영 및 운영 문서 동기화 완료
- 다음: 기능 추가 없이 안정화 점검(실행 로그/QA 보고서 델타 유지) 기준으로 운영 상태 고정
- 검증: `npm run build` PASS(`built in 6.57s`) + `check:mobile-qa:evidence` `READY_FOR_FINALIZATION` + `qa:mobile:finalize` `FINALIZED_PASS`

